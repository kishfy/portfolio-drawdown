import React from 'react'
import ReactDOM from 'react-dom/client'
import MonteCarloSim from './monte-carlo-sim'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MonteCarloSim />
  </React.StrictMode>,
)
